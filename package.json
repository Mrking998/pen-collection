{
  "name": "pen-collection-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-public.mjs",
    "dev": "pnpm build && wrangler dev --local",
    "deploy:dry": "pnpm build && wrangler deploy --dry-run",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
